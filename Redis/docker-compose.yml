version: '3.1'

services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: always
    ports: 
      - 6379:6379
    volumes:
      - ./redis-data:/usr/share/Redis/data

 
      
      